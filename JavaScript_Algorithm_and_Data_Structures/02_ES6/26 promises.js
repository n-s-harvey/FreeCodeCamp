/* 
? WHAT IS A PROMISE?
* Promise to do something, usually asynchronously
! Promise is a constructor function 
!   -> use new keyword to create one
* Takes a function as its argument, with two parameters:
*   resolve and reject
*   = methods used to determine outcome of the promise
 */

const makeServerRequest = new Promise((resolve, reject) => {

});
/**
 * * Principle of functional programming: declare your dependencies explicitly
 * *    if a function depends on an object or variable being present, pass it as an
 * *    argument
 */

// The global variable
let fixedValue = 4;

// Only change code below this line
function incrementer(value) {

    return value + 1;

    // Only change code above this line
}